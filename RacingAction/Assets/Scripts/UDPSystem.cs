using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UDPSystem : MonoBehaviour
{
    readonly int TIME = 10;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void Set(string recip, int recport, string sendip, int sendport)
    {
        recip = recip;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
